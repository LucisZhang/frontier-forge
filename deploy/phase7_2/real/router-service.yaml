apiVersion: v1
kind: Service
metadata:
  name: vllm-router
  namespace: forge-system
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vllm-router
  ports:
    - name: http
      port: 8080
      targetPort: http

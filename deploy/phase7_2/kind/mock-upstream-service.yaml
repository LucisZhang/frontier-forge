apiVersion: v1
kind: Service
metadata:
  name: forge-mock-upstream
  namespace: forge-system
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: forge-mock-upstream
  ports:
    - name: http
      port: 8000
      targetPort: http
